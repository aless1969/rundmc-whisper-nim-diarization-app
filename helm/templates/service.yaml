apiVersion: v1
kind: Service
metadata:
  name: {{ include "secure-code.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "secure-code.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secure-code.selectorLabels" . | nindent 4 }}
