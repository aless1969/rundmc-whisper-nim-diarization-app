apiVersion: v1
kind: Service
metadata:
  name: {{ include "rundmc-whisper-nim-diarization-app.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "rundmc-whisper-nim-diarization-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "rundmc-whisper-nim-diarization-app.selectorLabels" . | nindent 4 }}
